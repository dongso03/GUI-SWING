스윙


자원 (리소스)이 프로젝트에 포함되어있는지 적절한위치에 있는지가 중요하다.

jar파일로 익스포트했을때도 리소스파일경로가 중요하다.

jar파일로 만드는과정을 패키징이라고 부른다.
URL url = ClassLoaderFrame.class.getClassLoader().getResource("춘식17.png");
패키징할때 이미지파일도 포함되기에 따로 배포할필요가없다.

체크박스를 생성해 라벨이 아닌 체크가 가능하도록
라디오버튼도 마찬가지 지만 
그룹으로묶어서 한가지만 선택하게 가능

레이아웃으로 버튼 정렬 가능
레이아웃들
1.
 Flowlayout
=> 요소 추가한 순서 나열
 정렬

2.
GridLayout
영역을 행, 열로 나누어 동일한 크기로 사용
행이나 열의 수중 하나를 0으로 생략 가능 >>필요한 만큼 자동 조절됨.

3.
BorderLayout
한 요소가 한 영역을 모두 차지함.
 컨테이너에 추가 시, 영역을 문자열로 전달

새롭게 생성할때 스윙디자이너로 한번에 디자인가능 new>other >swingdeginer>jframe