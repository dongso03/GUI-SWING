클래스를 클래스 내부안에서도 정의가 가능하다
단 정의된 클래스 아래쪽에만 선언이 가능하다.
해당 메소드(클래스)에서 잠깐 사용하기위해 사용한다.

익명클래스
Comparator<Person> hei = new Comparator<Person>() {		
	};

사람 몸무게와 키가 있다고 필드생성하고 클래스를 여러개 늘리기보다 
한클래스안에서 위처럼 생성하고 그대로 사용가능

파라미터부분에 값이 아니라 동작을 넣고 실행가능
pro.showResult("문자열과", new MyOutput() { <<call back